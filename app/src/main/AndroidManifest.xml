<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventorynavigation">

    <application
        android:name="com.example.inventorynavigation.iu.InventoryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name="com.example.inventorynavigation.iu.InventoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.inventorynavigation.iu.signup.SignUpActivity" />
        <activity android:name="com.example.inventorynavigation.iu.login.LoginActivity" />
        <activity
            android:name="com.example.inventorynavigation.iu.SplashActivity"
            android:theme="@style/NoActionBar">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>